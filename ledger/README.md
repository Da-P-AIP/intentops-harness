# Ledger

This directory is reserved for generated Action Ledger entries.

The current MVP keeps ledger entries in browser memory so the demo can run without a backend. Future versions can persist JSONL files here.

